# 비밀 정보는 절대 저장소에 올리지 않는다
.env
.env.*
!.env.example

# 시스템/빌드
.DS_Store
node_modules/
.next/
out/
coverage/
*.tsbuildinfo
next-env.d.ts

# 로컬 도구 설정
.claude/settings.local.json

# Vercel CLI가 로컬에 만드는 프로젝트 연결 정보(비밀은 아니지만 기기별 로컬 설정)
.vercel
