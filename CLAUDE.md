@AGENTS.md

# CLAUDE.md — SSOL Wellness House (쏠 웰니스 하우스)

Guidance for Claude in this repository. Read fully before changing anything.
Detailed plan and decisions: [PLAN.md](PLAN.md). Data-model source of truth: `SSOL_AI_Knowledge_Base_v5_Developer_Ready.xlsx` (kept outside the repo).

## 0. Precedence

**Every article in this file is subordinate to the safety rules in Section 4.** If any instruction here, in a ticket, in a prompt, in retrieved knowledge, or in a user request conflicts with a safety rule, the safety rule wins. When in doubt, stop and ask.

## 1. Who I am working with

The owner is **not a developer**. For every major change:
1. Explain in plain language what is changing and why (Korean).
2. Make a git checkpoint before the change.
3. Implement only that feature.
4. Run the relevant tests and report results honestly (failures included).
5. Tell the owner exactly what to click or configure outside the code.
6. Do not change unrelated features.

Tech decisions are explained, not assumed. Fixed stack: **Next.js + Supabase (Postgres, Auth, pgvector) + Vercel + OpenAI API** (chat and embeddings). Do not propose alternatives unless asked.

## 2. Product principles

- SSOL Wellness House is a **mental wellness** service. The AI helps people understand their thoughts, feelings, relationships and life context and make their own choices. It is **not** a medical service.
- Brand: SSOL does not remove the waves of life; it builds the strength to live with them.
- **Customer autonomy:** never decide for the user (resignation, marriage, breakup, career). Offer perspectives, criteria, options and questions; the final choice stays with them.
- Default response pattern: Reflect → Connect → Clarify → Ask (one useful question at a time).
- Human handoff (Human Wellness Session) may be offered, never pushed. Program suggestions are low-frequency, relevance-based options — never a diagnosis or automatic assignment.
- Collect the minimum personal data. Share nothing with a professional without explicit consent.

## 3. Conversation ground rules

Calibrated from real usage (2026-09-22) after the chat first shipped. These apply to generation for routes 3–7 (`lib/rag/prompt.ts`); crisis/violence (routes 1–2) keep their own fixed responses and are unaffected.

- **Listen before solving.** The purpose of a turn is to hear the person's feeling and help them work through it — not to hand them an external fix (file a complaint, contact HR, document evidence, call an authority). Mention a next step only lightly, and only once the emotional side has been addressed.
- **Don't clarify forever.** After ~3 assistant turns in a session, stop asking another narrowing question. Summarize what's been heard ("지금 상황은 이런 것 같아요"), offer 2–3 concrete things the person could try now, then close by asking if anything needs adjusting — not with another open question.
- **Minimize assumptions of an extreme outcome.** Never bring up suicide, self-harm or "극단적 선택" on the model's own initiative in routes 3–7 — the safety router already escalates to `crisis` before generation runs if real signals are present, so generation never needs to double-check for it. Take everyday phrases ("그냥", "힘들다", "그만두고 싶다") at face value; don't read crisis into them.
- **"폭력" is not automatically the `violence` route.** Only actual physical danger (assault, sexual coercion, explicit threats, stalking, physical/coercive confinement) qualifies. "정신적 폭력", "언어폭력", "정서적 학대" and similar phrases without a physical-danger signal are `wellness`/`clinical_distress` — bug found and fixed 2026-09-22 (`남편이 정신적으로 저를 폭력적으로 대해요` was wrongly routing to the 112/119 fixed response). When a message is ambiguous between physical and non-physical harm, ask once to clarify before offering support — but don't re-ask once it's already been clarified as non-physical.
- **Plain text only.** No markdown (`**bold**`, `*` bullets, etc.) — this is a plain chat bubble UI, not a markdown renderer. Break into paragraphs (blank line) once a reply passes ~3 lines or shifts to a new sub-topic, for mobile readability.
- **No English framework jargon.** `frameworks.steps`/`framework_name` in the DB carry English acronyms (e.g. "WANT-CAN-NEED-ENOUGH"); strip these before they reach the user (`lib/rag/prompt.ts`'s `stripEnglishLabel`, and `purpose` instead of `framework_name`) and never let the model surface the acronym itself.
- **Suggestion phrasing, not refusal phrasing (2026-09-22).** Never say "저는 ~할 수 없어요/~해드릴 수 없어요/~하지 않아요". Say what to do instead, as a suggestion: "~해보는 건 어떨까요?", "~해볼까요?". The boundary itself (no diagnosis, no medication instructions, etc.) doesn't change — only the phrasing does. E.g. "저는 진단을 대신할 수 없어요" → "정확한 상태는 정신건강의학과에서 확인해보는 건 어떨까요?". Applies to `lib/rag/prompt.ts`'s instructions and to the fixed fallback strings in `lib/rag/generate.ts`/`app/api/chat/route.ts` — not to the crisis/violence fixed text in `crisisResponses.ts`, which is separately approved and untouched.
- **Suggestions split short-term vs. mid/long-term, ~3 total (2026-09-22).** When offering concrete things to try (self-care pairing, the ≥3-turn synthesis moment), split into something doable right now and something to keep up over time, about 3 examples total — not an undifferentiated list. `lib/rag/practicesSearch.ts` returns candidates from `wellness_practices` already split this way (tier `가볍게 시작` = short-term, `꾸준히 이어가기`/`장기 습관·정체성으로` = mid/long-term) so the model has real material for both buckets instead of inventing generic ones.

## 4. Critical safety hierarchy

Higher levels always override lower levels.

1. **Life and health safety** — suicide/self-harm, violence/coercion/stalking, acute medical symptoms, severe functional decline.
2. **Medical boundary** — no diagnosis, no medication instructions, no treatment claims.
3. **Legal, privacy, security** — consent, data protection, secrets.
4. **Customer autonomy.**
5. **Product experience and business goals** — personalization, programs, sales.
6. **Code style and convenience.**

Non-negotiable rules (source: `system_prompt` and `safety_rules` tables, SAFE-001…017):
- Crisis (SAFE-013) and violence/coercion (SAFE-001): **no retrieval, no sales, no program suggestions**; respond with pre-approved fixed text plus crisis resources. Crisis handling must work even if the AI provider is down and even when usage limits are reached.
- **Never diagnose** or label a condition ("~인 것 같아요" counts). Never infer a disorder from symptoms (SAFE-003).
- **Never instruct medication** start/stop/dose changes (SAFE-008). Refer to the prescribing doctor or pharmacist.
- **Never treat dessert type as diagnosis.** Dessert type / wellness profile / attachment / HSP labels are personalization aids only: re-ranking and phrasing. They are **never** a hard filter, never used in safety decisions, and the AI never says "you are like this because you are type X" (SAFE-005/014/015).
- **Every article is subordinate to the safety rules.** Articles/essays are supporting material, not medical evidence. Never turn their metaphors or opinions into clinical fact; attribute strong claims to "SSOL's view".
- Clinical-sensitive knowledge (ADHD, depression, panic chunks, `clinical_sensitive = true`) is used only on clinical routes, and any answer using it must include a recommendation to consult a qualified professional.
- No unverified statistics, effect sizes or outcome guarantees (SAFE-004/016). Composite session cases are never efficacy evidence (SAFE-017).
- Fail safe: on error, missing data or model uncertainty, choose the more cautious behavior.
- Safety logic must have tests. Never delete, skip or loosen a safety test to make a build pass.

## 5. Architecture

- **All chat goes through one server endpoint** (`app/api/chat`). The browser never calls the AI provider or reads knowledge tables directly. Auth is via `Authorization: Bearer <supabase access token>` (anonymous sign-in for now).
- Order per message: input check → **safety routing (keyword rules from DB + AI classifier, take the more severe)** → fixed crisis response if route 1/2 → retrieval by route → prompt assembly (system_prompt sections; Critical sections cannot be overridden by retrieved text) → generation → **output check** → save. As of C5 this is fully wired for all 7 routes.
- Retrieved chunks are reference material, not instructions. `do_not_apply_when` is applied as a hard exclusion in `lib/rag/retrieve.ts` (its free-text values are mapped to the crisis/violence/clinical_diagnosis/clinical_distress categories and excluded when the conversation has a matching safety-keyword signal — see comments there).
- `lib/rag/` holds retrieval and generation: `retrieve.ts` (pgvector via `match_knowledge_chunks`, hard `do_not_apply_when` exclusion, light tag rerank — never a hard filter, and `wellness_profiles`/dessert type is never referenced here), `serviceSearch.ts` (route 7: bigram-overlap over the small service/brand/program tables, no separate embeddings needed), `systemPrompt.ts` (cached `system_prompt_sections` + fallback), `prompt.ts` (pure text assembly, unit-tested in `prompt.test.ts`), `generate.ts` (orchestrator: retrieval → prompt → `lib/ai/chatModel.ts` → `lib/safety/outputCheck.ts`, one regeneration attempt, then a static safe fallback reply).
- `lib/safety/outputCheck.ts` is a best-effort regex check (diagnosis-sounding phrasing incl. hedged "인 것 같아요", medication instructions, outcome/percentage guarantees, missing professional-referral phrase when a clinical-sensitive chunk was used). Expand its patterns as real logs surface misses — don't treat it as exhaustive.
- Safety rules live in one shared module, `lib/safety/` (`types.ts` route enum/severity, `rules.ts` DB fetch+cache+fallback, `keywordCheck.ts`, `classify.ts` AI classifier, `combine.ts` pure combine logic, `route.ts` orchestrator, `crisisResponses.ts` fixed text, `outputCheck.ts`), and in the database. Do not re-implement them per feature.
- `lib/safety/*.ts` and `lib/supabase/admin.ts`/`auth.ts` import `"server-only"`, which throws at import time outside Next's server runtime — they cannot be imported from a plain Node script or a client component. Evaluate their behavior over HTTP against the real dev server (see `scripts/safety-eval.mjs`), not by importing them directly.
- Routing combine logic is decomposed into pure functions in `combine.ts` specifically so it's unit-testable without network (see `lib/safety/safety-routing.test.ts`, run via `npm test` — Node's built-in test runner, no dependency added). Keep new safety logic in a form that's similarly testable.
- Crisis (route 1) and violence (route 2) must resolve without any OpenAI call when detected by keyword — this is what keeps crisis handling working even if the AI provider is down. Don't add a network call on that path.
- All AI-provider calls go through one file (`lib/ai/`) so the provider can be swapped.
- Knowledge tables (from Excel) are readable only with the server key. User tables use Row Level Security (own rows only); chat writes are server-only.
- Knowledge source of truth is the Excel file. Regenerate SQL with `perl scripts/build_import_sql.pl <xlsx> supabase/import`; do not hand-edit generated SQL.
- Prompts live in files/DB rows, not inline strings.
- **The Supabase project is shared with the owner's quiz/payment prototype** (tables and functions prefixed `ssol_`, e.g. `ssol_orders`, `ssol_chat_passes`, `ssol_quiz_results`, `ssol_reports`). Never modify, drop or grant on `ssol_*` objects. Our tables are unprefixed. New tables in this project do **not** get `service_role` privileges automatically — every migration must include explicit GRANTs. Auth (`auth.users`) is shared, so anonymous sign-in and our `on_auth_user_created` trigger affect both apps.

## 6. Decisions already made

- Audience: Korean language, Korean residents, adults (19+), self-declared for the MVP.
- Chat history retention: delete on user request/withdrawal; auto-delete 12 months after last use; raw text reused for improvement only with separate consent or de-identification. Payment/dispute records follow 전자상거래법 (5y/3y) when payments exist. Legal review is pending before public launch.
- Consent types are separate: service, sensitive data, overseas transfer, marketing (optional, default off).
- AI cost: max 3,000 KRW per user per month. **Daily message limit: 20/user/day, decided 2026-09-22** after the cost simulation showed even 30/day stays under the cap (`lib/safety/dailyLimit.ts`, `DAILY_MESSAGE_LIMIT` env-overridable; day boundary is KST midnight). Applies only to routes 3–7 (the ones that call the generation model) and only blocks after the limit is already hit that day — crisis/violence/classifier-unavailable responses are checked and returned earlier in `app/api/chat/route.ts` and are never subject to this limit.
- Dessert-type quiz scoring: highest domain score wins; ties by domain order relate → worth → control → happy → meaning; within the domain, F vs E sum; tie → F. Only scores and type are stored, never per-question answers.
- **Current MVP scope: only the AI chat** (logo, chat screen, input, answers, new chat). Not yet: quiz, payments, subscriptions, booking, reports, memory, login screens. **Superseded 2026-09-22** by the decision below — real login and a 3-tab app shell are now being built.
- **2026-09-22 decision — real login replaces anonymous auth; accounts shared with the existing ssolwellness.com quiz/payment product.** The chat app's login is the *same* Supabase Auth account as `ssol_profiles`/`ssol_quiz_results`/`ssol_reports` (shared `auth.users`) — a user who already paid for a report on the other site logs into this app with the same email/password and sees it in 마이페이지 (read-only access to those `ssol_*` tables; still never write to them, per the shared-project rule above). Login methods: email+password (implemented), Kakao (native Supabase provider, needs enabling in Supabase Auth dashboard), Naver (not implemented anywhere yet — added as a custom-OAuth-provider call with provider name `"naver"`; only works once the owner registers a custom OAuth provider of that exact name in Supabase Auth). `components/auth/AuthGate.tsx` gates the whole app on a *real* session — it explicitly checks `user.is_anonymous !== true` and signs out any leftover anonymous session, otherwise an old anonymous session (or a future accidental one) would bypass the login screen entirely. `lib/supabase/browser.ts`'s old `ensureAnonymousSession()` is unused by the app now (kept for reference/scripts) — screens use `getAccessToken()` instead, which assumes a real session already exists.
- **App is now a 3-tab shell** (`components/app/AppShell.tsx`, post-login): 홈 (greeting + daily practice suggestion), 채팅 (existing `ChatApp`, now content-only — no longer its own full-page shell), 마이페이지 (dessert-type/report/profile/counselor-handoff). All three tabs stay mounted and are shown/hidden with CSS so switching tabs never loses in-progress chat state. Logged-out state shows `components/auth/LoginScreen.tsx`, combining the ssolwellness.com homepage's real marketing copy ("이유 없는 불안의 다스림, 삶과 진로에 대한 확신이 필요하신가요? 성공과 행복 그 이상의 당신을 찾아드립니다.") with the login/signup form — verified against the live site 2026-09-22, not paraphrased.
- **Dessert-type mapping for reading `ssol_quiz_results`**: `type_key` there (e.g. `"happy_E"`) is `{domain}_{F|E}` and maps to our `dessert_type` codes via the table already recorded in PLAN.md §7-9 (e.g. `happy_F`→`yogurt_parfait`, `happy_E`→`espresso_brownie`). Use that table, don't re-derive it.
- **Resolved 2026-09-22 — signup email "failure" was a false alarm from test data, not a config problem.** `signUp()` returned "Error sending confirmation email" (500) while testing with `@example.com` addresses — traced via the Resend dashboard's Logs tab to Resend's own validation error: `"Invalid to field. Please use our testing email address instead of domains like example.com"`. Resend (already correctly configured as custom SMTP, verified sending domain, "Confirm email" left on) rejects the RFC 2606 reserved documentation domain `example.com` specifically. Retested with a real, receivable domain (`@mailinator.com`) — signup succeeded with no error. Real signups (gmail.com, naver.com, etc.) work correctly. Nothing to change in the owner's Supabase/Resend setup. Lesson for future testing in this repo: never use `@example.com` in live signup tests against this project — use a real or `mailinator.com`-style receivable domain instead.
- **2026-09-22 addition — 마이페이지 (`/api/mypage`, `MyPageTab.tsx`)**: three read-only sections. "내 유형 열람하기" maps `ssol_quiz_results.type_key` (e.g. `"happy_E"`) to our `dessert_type` via `lib/mypage/dessertTypeMap.ts` (copied from PLAN.md §7-9's already-verified table, not re-derived) and shows the matching `persona_profiles` entry plus the raw `domain_scores`; underneath it, if a `ssol_reports` row with `status='ready'` exists for the user, its `sections` ({title, body} — AI-generated, markdown-styled) render through a small dependency-free "lite markdown" renderer (`**bold**` and `- ` bullets only — a real report is closer to a document than a chat bubble, so unlike the chat's plain-text rule, minimal formatting is kept). "내 정보 확인" shows `profiles.display_name` (falling back to `ssol_profiles.name`), auth email, and `ssol_profiles.gender`. "상담사 연결" is a placeholder external link to `ssolwellness.com` — the owner said this stays an outbound link for now; **replace the URL once a real booking page exists**. All three `ssol_*` reads are SELECT-only, per the shared-project rule. Verified against the owner's own real account (`sugar0401pie@gmail.com`, which already has a quiz result and an 8-section report in this project) via direct DB queries, and against a fresh throwaway account for the "no test taken yet" empty state via the live UI.
- **2026-09-22 bug fix — `todayKeyKST()` added**: `startOfTodayKST()` returns a `Date` representing KST midnight *as a UTC instant*; calling `.toISOString().slice(0, 10)` on it (as the first draft of `/api/daily-practice` did) re-reads that instant's UTC calendar date, which is one day behind KST evenings/late-day — e.g. it showed "9월 21일" on the actual KST date 2026-09-22. `lib/safety/dailyLimit.ts` now also exports `todayKeyKST()`, which computes the KST year/month/day directly. Use this (not `startOfTodayKST().toISOString()...`) anywhere a KST calendar-date *string* is needed.
- AI models (2026-09-21): embeddings `text-embedding-3-small` (1536-d); safety classifier and answer generation start with `gpt-5.6-luna`, kept configurable (env) so `gpt-5.6-terra` can replace it after the safety evaluation. OpenAI monthly cost cap is enforced by prepaid credits (auto-recharge off).
- Retrieval: embedded text per chunk = title + text + use_when + issue tags (better than text alone). Initial `min_similarity` ≈ 0.33 (tune with logs); similarity alone cannot reject off-topic questions, so combine with the classifier's topic/domain output.
- **Not yet built, needed before any public/Vercel deployment**: an access-code gate (so the open internet can't reach `/api/chat` and run up OpenAI cost before consent screens/legal review exist), and per-user/day message limits (crisis responses must stay exempt from any limit). Currently the app only runs locally.
- **Measured cost (2026-09-22, `npm run cost:sim`, real pipeline, 12 messages across routes 3–7)**: ~679 in/93 out tokens for the classifier, ~1819 in/271 out for generation, per message. At gpt-5.6-luna pricing ($0.20/$1.20 per MTok) that's **≈1.3 KRW/message** (1400 KRW/USD). Monthly per user: 5/day≈197원, 10/day≈393원, 20/day≈787원, 30/day≈1,180원 — all comfortably under the 3,000원/month cap. A regeneration (output-check failure, retried once) roughly doubles that message's cost; observed in ~1/12 real messages. gpt-5.6-terra would be ~10x (same token counts assumed, not verified). Daily-message-limit value still to be decided by the owner using this data.
- **Observed routing nuance**: the safety classifier isn't perfectly deterministic between similar phrasings of ambiguous mood complaints (e.g. "요즘 공허해요" vs "공허하고 행복하지 않은 기분이에요") — one tested as `wellness`, a paraphrase tested as `clinical_distress`. This is the approved "escalate when ambiguous" behavior working, not the weak-keyword bug (already fixed, see below). Effect: retrieval then searches only the 15 clinical-sensitive chunks, which may fit poorly for a genuinely non-clinical complaint — but `generateAnswer`'s "reference only, don't parrot" instruction kept the actual reply on-topic and safe in the case observed. Worth watching in real usage logs; not something to chase further without data.
- **2026-09-22 bug fix — medical-boundary over-triggering**: common single words in `safety_rules.trigger` ("무기력", "진단", "치료", "중단", "용량", "부작용") were forcing completely unrelated everyday sentences into `clinical_diagnosis`/`clinical_distress`. Two layers were involved: (1) `lib/safety/keywordCheck.ts`'s `WEAK_TRIGGERS` excludes these words from the keyword-forced `moreSevere` escalation (already existed for one case; extended here); (2) `lib/safety/classify.ts`'s `routeGuide()` used to list these same weak words as bare examples in the classifier's own prompt, which made the classifier itself misjudge single-word mentions (e.g. "오늘 하루 종일 좀 무기력하네요") — fixed by excluding weak-trigger words from the example list and adding explicit context-requirement sentences to the clinical_diagnosis/clinical_distress/wellness route descriptions. Both layers were needed; fixing only the keyword layer left the classifier's own judgment still wrong.
- **2026-09-22 decision — don't repeat the medical-boundary disclaimer every turn**: owner feedback was that "정신건강의학과 등 전문가 상담을 받아보세요, 저는 진단을 대신할 수 없어요" was appearing on every single clinical-route turn, even within the same conversation. Declined the owner's proposed workaround (gate the disclaimer behind "5+ occurrences of medical/violence questions") because it would weaken genuine first-disclosure safety behavior for a real Critical-priority rule (SAFE-007/008/009 always require it). Instead: `chat_sessions.clinical_boundary_stated_at` (migration `20260922000900`) records the first time in a session the disclaimer was actually sent; `generateAnswer` reads it and passes `clinicalBoundaryAlreadyStated` into `buildSystemPrompt` (switches the instruction from "must include referral" to "don't repeat it, offer concrete self-care suggestions instead") and into `checkOutput` (only requires the referral phrase to be present on the first occurrence per session). Every clinical-route turn — first time or not — also now instructs the model to pair the safety language with 1–2 concrete self-help/mood-lifting suggestions, addressing the "just tell me to see a doctor with nothing else" complaint without removing the referral itself. Falls back safely to "always show the disclaimer" behavior if the migration hasn't been run yet (the column-not-found Supabase error just leaves `clinicalBoundaryAlreadyStated` false).
- **2026-09-22 addition — owner-provided wellness practices DB grounds concrete suggestions**: owner supplied `wellness_practices.json`/`.xlsx` (375 items: domain × category × tier — domains 나 자신/인간관계/연인관계·부부생활/회사·커리어/육아; categories 몸으로 움직이기/생각 전환하기/관계·대화로 풀기/나를 돌보기/도움 요청하기; tiers 가볍게 시작/꾸준히 이어가기/장기 습관), kept outside the repo like the main Excel knowledge base. Imported via `scripts/build_practices_sql.mjs <json path>` (regenerate, don't hand-edit) into a new `wellness_practices` table (migration `20260922001000`). `lib/rag/practicesSearch.ts` picks up to 4 candidates per message (bigram overlap on title/detail/category + a domain-keyword-guess bonus, no embeddings — same reasoning as `serviceSearch.ts`'s small-table approach), deliberately bucketed by tier (top 2 `가볍게 시작` + top 1 `꾸준히 이어가기` + top 1 `장기 습관·정체성으로`, 2026-09-22) rather than a single ranked list — a plain score sort lets short-term items (25/domain) crowd out mid/long-term ones, which would break the "단기+중장기 섞어서 제안" ground rule below — for routes `wellness`/`life_decision`/`clinical_distress` (not `clinical_diagnosis` — that route is about diagnosis/medication questions, not activity suggestions; not persona-question mode — that's character interpretation, not advice). `prompt.ts` includes them as reference material the model should prefer over inventing generic suggestions, but never as a prescription. Falls back to no suggestions (not an error) if the migration hasn't been run yet.
- **2026-09-22 fix — the daily-limit/classifier-unavailable messages referenced a help-contact area that doesn't exist**: both said "화면 아래 도움 연락처" (the help contact at the bottom of the screen), but the footer shows the brand vision line instead (see the 2026-09-22 footer decision above) — there is no such contact area. Fixed by adding `SHORT_HELP_CONTACT` (`lib/safety/crisisResponses.ts`, reuses the same approved 109/1577-0199 numbers) and including it directly, restrained to a single line — not the full 4-part crisis structure — only in these two system-fallback messages (`app/api/chat/route.ts`). A genuine crisis message is unaffected either way: it's caught by keyword/classifier before the daily-limit check ever runs.
- **2026-09-23 — first production deployment**: GitHub repo `sugar0401pie-maker/Ssol-Wellness-House`, deployed on Vercel, live at `app.ssolwellnesshouse.com` (CNAME added in Cloudflare, which is this domain's actual DNS host — its nameservers already pointed there before this project). `ACCESS_CODE` is set for real now (no longer optional-off) since this is publicly reachable. **Gotcha hit during setup**: adding/changing environment variables in the Vercel dashboard does not affect an already-built deployment — Vercel's dashboard "Redeploy" also didn't reliably pick up newly-added `NEXT_PUBLIC_*` values (a build cache or Turbopack incremental-cache issue produced byte-identical JS chunk hashes across several dashboard redeploys even after the vars were confirmed present). What reliably worked was pushing a new commit and letting Vercel's Git integration build fresh. If `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` ever seem "set but not working" in production (symptom: the login screen shows "설정 오류로 로그인을 사용할 수 없어요" while server-only vars like `ACCESS_CODE` work fine), a plain dashboard redeploy is not proven reliable here — push a trivial commit instead. **Correction, same day**: the actual root cause that day was simpler than a caching bug — the `NEXT_PUBLIC_*` values were genuinely empty in Vercel (the owner had added the variable names but never typed a value in). Every "redeploy" was faithfully rebuilding an empty value, which is why the JS output kept coming out byte-identical. The "push a trivial commit" workaround still did the job (it's what finally produced a build with the real value), but don't assume Vercel's cache is unreliable by default — check that the Value field actually has content first.
- **2026-09-23 — real signup flow, matching ssolwellnesshouse.com's own signup screen**: replaced the plain email+password signup with the same shape as the sibling site's screen (이름/생년월일 입력 → 이메일로 인증번호 받기 → 인증번호+비밀번호 입력 → 약관 동의 → 가입 완료). Implementation: `sendSignupOtp()` calls `signInWithOtp({ email, options: { shouldCreateUser: true, data: { display_name, birth_date } } })` — this both sends a numeric code by email (confirmed via Resend logs: the confirmation email already includes `인증번호: <8 digits>` alongside the link, so no separate email template work was needed) and stashes the name/birthdate as `user_metadata`; `verifySignupOtp()` calls `verifyOtp({ email, token, type: "email" })`, which logs the user in; `finishSignup()` then calls `updateUser({ password })` and writes `display_name`/`birth_date`/`terms_agreed_at`/`sensitive_data_agreed_at` into `profiles` (migration `20260923000000_signup_fields.sql` adds those three columns — **owner needs to run this migration**, `finishSignup`'s profile-save step 500s until they do). Phone-number signup is intentionally not offered yet (no SMS provider configured, per PLAN.md §7-9) — the form shows email only with a "휴대폰 인증은 준비 중" note. The plain `signInWithEmail`/`signUpWithEmail` functions in `authClient.ts` are kept for the login tab and for scripts, just no longer used by the signup UI.
- **2026-09-23 — cross-domain session sharing with ssolwellnesshouse.com**: `lib/supabase/browser.ts` switched from plain `@supabase/supabase-js` `createClient` (localStorage sessions, origin-scoped) to `@supabase/ssr`'s `createBrowserClient` with `cookieOptions.domain` set via `lib/supabase/shared-cookie-domain.ts` to `.ssolwellnesshouse.com` whenever the page is served from that root domain or a subdomain of it (returns `undefined` — default host-only cookie — for `localhost`/`*.vercel.app`, since browsers reject setting a cookie domain that doesn't match the real host). This lets a session cookie set by this app be read by the sibling ssolwellnesshouse.com quiz app (and vice versa) since both are on the same parent domain, once that sibling app applies the identical logic on its side (tracked there, not in this repo). Nothing else needed to change — `getAccessToken()`/`getSession()` work the same regardless of storage backend.
- **2026-09-24 bug found — `profiles` table never had UPDATE granted to `authenticated`.** The original schema (migration `20260921000000`) gave `authenticated` only `select` on `profiles`; the `profiles_update_own` RLS policy existed but a GRANT and an RLS policy are both required, so any client-side update silently affected zero rows with no error (Postgres RLS filters to nothing; only a direct psql/service-role test surfaced the real underlying "permission denied for table profiles"). Never noticed before because nothing wrote to `profiles` from the browser client until `finishSignup()`. Fixed by migration `20260924000000_profiles_update_grant.sql`. `authClient.ts`'s `finishSignup()` now also does `.update(...).select("user_id")` and treats zero returned rows as a failure, so a similar future permission gap surfaces as a real error instead of silently no-op'ing.
- **2026-09-24 — mandatory "tell us about yourself" onboarding after signup/login**: matches the owner's ssolwellnesshouse.com wording exactly ("자기 자신에 대해 알려주세요 / tell us about yourself for us to understand you better"). `profiles.onboarding_completed_at` (migration `20260924000100`) gates it — `AppShell.tsx` calls `GET /api/onboarding` on mount and, if not completed, renders `OnboardingModal` over the whole shell (no close button — all 3 tabs stay hidden underneath until answered, per "반드시 먼저 체크하도록"). Two questions, both required: "즐거움을 느끼는 부분" (활동 목록, mostly exercise-related) and "주요 고민거리" (연애/학업/직장 및 커리어/일상/육아/가족/기타). `lib/onboarding/mappings.ts` maps these onto `wellness_practices.category`/`.domain` respectively (`ENJOYMENT_TO_CATEGORY`, `CONCERN_TO_DOMAIN` — an approximation, not a 1:1 fit for "학업"/"가족" since `wellness_practices` only has 5 domains) and `getDailyPractice()` (`practicesSearch.ts`) now filters its "가볍게 시작" pool by that domain+category combo when set, falling back to domain-only then to the full pool — this directly fixes the "육아와 무관한데 육아 조언이 나온다" complaint, since previously the daily pick ignored domain/category entirely. The nickname shown in the modal is read-only (pulled from `profiles.display_name` → `ssol_profiles.name` → `ssol_quiz_results.user_name`) with a 공주/왕자/공작 title guessed from gender — changing it happens in 마이페이지 → 내 정보 확인 (new `PATCH /api/profile`), not in the modal.
- **2026-09-24 — 상담사 연결 is a request form, not an outbound link**: owner decided not to open a real booking page yet. `POST /api/counselor-inquiry` inserts into a new `counselor_inquiries` table (migration `20260924000200`, service-role only — no RLS policy for users yet since nothing reads it back client-side) with the contact info + optional message; MyPage's counselor section is a small form instead of a link, and owner checks the table manually for now.
- **2026-09-24 — prompt tuning**: two things repeated too often in real conversations. (1) Restating the user's own words back for empathy every single turn — now `prompt.ts` tells the model (turnCount ≥ 1) not to keep re-summarizing feelings already reflected, and move on. (2) Naming a feeling/situation abstractly ("배제감", "부당함", "무력감") and clarifying each one individually before offering anything — refined twice: first told to just bundle generic suggestions, then the owner clarified further — most concerns are made of several feelings at once, so don't hold off with more clarifying; name each feeling that came up **and** pair it with a concrete thing to do for that one, all in the same reply (e.g. "배제감엔 이렇게, 부당하다고 느껴질 땐 이렇게, 무력감이 들 땐 이렇게").
- **2026-09-24 — quiz overhauled to v2, superseding several 2026-09-21/22 decisions above**: the owner shared `final-ssol-wellness-v2-master-spec.md` for the *sibling* quiz app (built/maintained in a separate repo, not this one — this repo only consumes its output via the shared `ssol_quiz_results` table). The whole taxonomy changed: **5 domains are now CAR/LOV/REL/SLF/DIR (커리어/연애/관계/나 자신/삶의 방향)**, replacing the old relate/worth/control/happy/meaning — `lib/wellness/domainLabels.ts` updated. **Score meaning flipped**: v1 scores were "총량, 높을수록 그 영역 언급이 많았다"; v2 scores mean "그 영역이 얼마나 건강하게 채워져 있나" (higher = healthier) and the **main concern is the *lowest*-scoring domain** (opposite of v1's highest) — `generate.ts`'s `summarizeThemeScores` labels now say "비교적 안정적"/"요즘 더 신경 쓰이는 영역" instead of bare "가장 높음"/"가장 낮음" to reflect this. **Types went from 10 (domain × F/E) to 15 (domain × coping mode: 1차통제/2차통제/이탈)**, with `type_key` format changing from `"happy_E"` to `"CAR-primary"` — `lib/mypage/dessertTypeMap.ts` replaced entirely (7 dessert names reused, 8 new: 아포가토/딸기쇼트케이크/베리치즈케이크/베리소르베/바스크치즈케이크/마들렌/레몬머랭타르트/프루트타르트) and `persona_profiles` replaced with 15 new rows (migration `20260924000300`, content assembled only from the spec's own TYPE_LINE1/TYPE_LINE2/family-description text — nothing invented, per SAFE-005). **Consequence for existing data**: any `ssol_quiz_results` row from before this change has an old-format `type_key` (e.g. the owner's own test result, `"relate_F"`) that isn't in the new map, so `loadQuizPersona()` correctly returns "no persona yet" for it — those users need to retake the v2 test for their persona to show up anywhere in this app.
- **2026-09-24 — found and fixed: chat's persona hint was reading the wrong table.** `generate.ts`'s `computePersonaHint()` used to read `wellness_profiles.dessert_type`, but nothing in production ever writes to that table from a real quiz result — only my own manual test scripts had ever populated it. `app/api/mypage/route.ts` had already been reading `ssol_quiz_results` directly (correctly) since checkpoint 16. Consolidated both onto a new shared `lib/mypage/loadQuizPersona.ts` that reads `ssol_quiz_results` + `persona_profiles`; `wellness_profiles` is no longer read for this purpose (table itself left in place, unused, in case a future feature wants it).
- **2026-09-24 — `wellness_practices.domain` recategorized to match the v2 quiz's 5 domains** (migration `20260924000400`): 인간관계→관계, 연인관계·부부생활→연애, 회사·커리어→커리어, 육아→관계 (v2's "관계" domain explicitly includes family), 나 자신 unchanged. Only the `domain` label changed — `id`/`category`/`tier`/`title`/`detail` untouched. **Known gap**: no existing practice content maps to "삶의 방향" (none of the 5 old domains corresponded to it), so that domain has zero dedicated items — `getDailyPractice()`'s existing fallback (domain-match → full pool) degrades gracefully rather than breaking, but a "삶의 방향"-themed practice set would need to be authored separately. `lib/onboarding/mappings.ts`'s `CONCERN_OPTIONS` changed to match exactly: 커리어/연애/관계/나 자신/삶의 방향/기타 (dropped the old ad-hoc 학업/일상/가족 options now that the real 5-domain taxonomy is available). `practicesSearch.ts`'s `DOMAIN_KEYWORDS` (used to guess domain from chat text) updated the same way.

## 7. Coding conventions

- TypeScript, Next.js App Router. **This Next.js version differs from older versions: read the relevant guide in `node_modules/next/dist/docs/` before using an API.**
- Follow the existing style; run `npm run lint` and `npm run build` before finishing.
- Small, focused functions and commits. One concern per change. Comment the *why*, especially safety decisions.
- Handle errors explicitly; safety-relevant failures fail closed.
- Add or update tests with every behavior change; safety paths need tests for the unknown/missing-data case.
- Korean UI text in plain language, no medical claims. Font: Noto Sans KR. Mobile-first.
- Korean IME: never treat Enter as "send" while `isComposing`.
- Local dev port: **3100** (port 3000 is used by the owner's other project — do not stop it).
- Commit messages: imperative, explain the why.

## 8. Secrets and privacy

- **Never expose API keys, tokens or passwords** — not in source, tests, comments, logs, error messages, commits, screenshots, prompts or client bundles.
- Secrets live only in `.env.local` (git-ignored) and Vercel environment variables; the owner enters them, never pasted into chat. Never read or print their values; refer to them by name.
- The Supabase service-role key and the OpenAI key are server-only. Only `NEXT_PUBLIC_*` values may reach the browser, and only non-secret ones.
- Do not log message content, health information or personal data in analytics or debug logs.
- If a secret appears anywhere, stop, tell the owner, and recommend rotating it.

## 9. Before finishing a task

- [ ] Only what was asked changed; no unrelated features touched.
- [ ] No secrets added; `.env*` not committed.
- [ ] No diagnosis/treatment claims; dessert type never used as health inference or hard filter.
- [ ] Crisis path and medical-boundary behavior unchanged or improved, and tested.
- [ ] `npm run lint`, `npm run build` and tests pass (or failures are stated honestly).
- [ ] Checkpoint committed; owner told what to do next outside the code.
