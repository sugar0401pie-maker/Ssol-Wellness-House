# SSOL Wellness House — Production Web App 계획서 (Plan Only)

작성일: 2026-09-21 · 상태: **검토용 초안 (코드 없음)**
분석 대상: `SSOL_AI_Knowledge_Base_v5_Developer_Ready.xlsx` (17개 시트)

> 이 문서는 "무엇을, 왜, 어떤 순서로 만들 것인가"만 다룹니다. 승인 전에는 코드를 작성하지 않습니다.
> 모든 항목은 `CLAUDE.md`의 안전 원칙(Section 3)에 종속됩니다.

---

## 0. Excel 분석 요약

### 0-1. 이 Excel이 실제로 담고 있는 것

| 시트 | 내용 | 앱에서의 역할 |
|---|---|---|
| system_prompt | 13개 섹션(IDENTITY~BRAND). 10개가 `Critical` | AI의 고정 지침. RAG 결과가 절대 덮어쓸 수 없음 |
| safety_rules | SAFE-001~017. 트리거 키워드가 `|`로 구분됨 | 안전 라우터의 규칙 데이터. **코드에 박지 않고 DB에서 읽음** |
| response_routing | 7단계 우선순위 (자살/자해 → 폭력 → 진단·약물 → 우울/공황 → 인생결정 → 일상웰니스 → 서비스문의) | 사용자 메시지를 어느 경로로 보낼지 결정 |
| knowledge_chunks | 82개 chunk / 19개 article. evidence_level: High 26, Medium 53, Medium-High 1, Medium-Low 1, **Needs verification 1** | RAG 검색 대상. 이 chunk만 임베딩 |
| programs (5) + program_routing_rules (5) | 프로그램 5종과 추천 규칙 | 관련성 설명용 선택지. 진단·자동 배정 금지 |
| service_knowledge (10), brand_knowledge (8) | 공식 서비스/브랜드 문구 | "SSOL이 뭔가요?" 류 질문의 정답 원천 |
| db_schema | 테이블/컬럼 초안 (74행) | Supabase 테이블 설계의 출발점 (불완전, 아래 참고) |
| frameworks(11), question_library(28), taxonomy, session_examples(5), source_policy(7), developer_config(13) | 보조 지식·설정 | 질문 선택, 태그 필터, 출처 우선순위, 튜닝값 |

### 0-2. 데이터 자체가 정하고 있는 핵심 규칙 (앱이 그대로 따라야 함)

1. **우선순위**: 안전/임상 정책 > 서비스·브랜드 > 프레임워크 > article > 사용자 프로필 (`source_policy`, `developer_config.retrieval_priority`)
2. **디저트 유형은 진단이 아님**: 재랭킹·표현 개인화에만 사용. 검색 hard filter 금지 (SAFE-005, `developer_config.profile_use`)
3. **최우선 override**: 자살/자해는 일반 article 검색·판매·프로그램 추천 모두 금지 (`response_routing` 1번)
4. **article은 원문 재생산 금지**: chunk로만 검색, 은유·철학을 임상 사실로 바꾸지 않음
5. **검증 안 된 통계 노출 금지** (SAFE-004, SAFE-016). programs의 `evidence_status`가 전부 "verify statistics"
6. **가상 사례는 효능 증거 아님** (SAFE-017)
7. **AI 대화를 전문가에게 자동 공유 금지**. 명시적 동의 필요 (`human_chat_share`)
8. **기억(memory)**: 요약 500~1000자 + 최근 5~10개 메시지만. 전체 히스토리 전송 금지

### 0-3. Excel에서 발견한 문제/빈칸 (개발 전에 결정해야 함)

| # | 발견 | 왜 중요한가 | 제안 |
|---|---|---|---|
| 1 | **위기 상황 연락처/안내 문구가 Excel에 없음.** SAFE-013은 "지역 응급/위기 자원 연결"이라고만 함 | 가장 중요한 응답인데 내용이 비어 있음 | 위기 응답 문구와 연락처를 별도 시트로 작성하고 **전문가(정신건강 전문가)가 검수**. 전화번호는 개발 시점에 공식 출처로 재확인 |
| 2 | `db_schema`에 `chat_logs`와 `chat_sessions/chat_messages/retrieval_logs`가 겹침 | 같은 정보를 두 곳에 저장하게 됨 | `chat_sessions + chat_messages + retrieval_logs`로 통합 (아래 제안) |
| 3 | `db_schema`는 **일부 컬럼만** 나열 (예: knowledge_chunks에 `use_when`, `do_not`, `do_not_apply_when` 없음) | 그대로 만들면 안전 정보(do_not 등)가 빠짐 | 실제 시트의 모든 컬럼을 테이블에 반영 |
| 4 | `do_not_apply_when`(예: 폭력 관계에서는 이 chunk 사용 금지)이 **14개 chunk에만** 있음 | 이 필드는 검색 후 반드시 적용해야 하는 안전 필터 | 검색 결과에서 코드로 강제 제외 |
| 5 | `rag_min_similarity`가 `TUNE_IN_PROD` (미정) | 유사도 기준 없이는 엉뚱한 chunk가 답에 섞임 | 초기값은 보수적으로 두고 테스트 로그로 조정 |
| 6 | "Needs verification" chunk 1개, programs의 통계 문구 전부 미검증 | SAFE-004/016 위반 위험 | 해당 chunk는 기본 검색 제외 플래그, 프로그램 통계는 화면·답변에 노출 안 함 |
| 7 | 개인정보: 정신건강 관련 대화는 민감정보 성격 | 한국 개인정보보호법상 별도 동의 등 요건이 있을 수 있음 | **출시 전 법률 검토 필수** (저는 법률 자문 불가) |
| 8 | README의 컬럼 정렬이 어긋남 (표시 문제) | 데이터 영향은 작음 | 무시 가능 |

---

## 1. 앱 전체 Architecture

### 1-1. 한눈에 보기 (비개발자용 설명)

- **Next.js** = 화면(웹사이트)과 "서버 기능"을 한 프로젝트에서 만드는 도구. 사용자가 보는 채팅 화면과, 뒤에서 AI를 부르는 기능이 함께 들어 있습니다.
- **Supabase** = 데이터 창고 + 로그인. 회원, 대화 기록, SSOL 지식(chunk)을 저장합니다. pgvector라는 기능으로 "의미가 비슷한 글 찾기"를 합니다.
- **Vercel** = 만든 웹사이트를 인터넷에 올려주는 호스팅.
- **AI API** = 답변을 만드는 두뇌(외부 서비스). **API 키는 서버에서만 사용, 화면(브라우저)에는 절대 노출 안 함.**

```
[사용자 브라우저]  ── HTTPS ──▶  [Vercel: Next.js]
   채팅 화면                       ├─ 화면(페이지)
                                   └─ 서버 기능 /api/chat  ◀── 여기가 "안전 라우터 + RAG"의 본체
                                          │
                       ┌──────────────────┼───────────────────┐
                       ▼                  ▼                   ▼
                [Supabase]          [AI API]            (나중) 예약/결제 등
                 - 로그인(Auth)      - 분류/생성
                 - 지식 테이블       - 임베딩
                 - 사용자 데이터
                 - pgvector 검색
```

### 1-2. 핵심 설계 원칙 (전부 안전 우선)

1. **모든 채팅은 서버 한 곳(`/api/chat`)을 통과**합니다. 브라우저가 AI나 지식 DB에 직접 접근하지 못하게 합니다 → 안전 검사를 우회할 수 없음.
2. **안전 규칙은 데이터(DB) + 하나의 모듈**로 관리. 기능마다 따로 구현하지 않음.
3. **위기 응답은 AI가 즉석에서 쓰지 않고, 미리 검수된 고정 문구**를 사용 (AI 응답 실패·오작동과 무관하게 동작).
4. **지식 원본은 Excel** → 검증 후 DB로 import. 앱에서 직접 편집하지 않음 (MVP). 변경 이력은 `content_version`으로 추적.
5. **개인정보 최소 수집**, 화면에 보이는 사용자 데이터는 본인 것만 (Supabase Row Level Security).

### 1-3. 화면(페이지) 구성 — MVP

| 화면 | 설명 |
|---|---|
| 랜딩/소개 | SSOL 서비스 소개, "의료서비스가 아닙니다" 명시 |
| 로그인/가입 | 이메일 로그인부터 시작 |
| 온보딩·동의 | 서비스 범위, 의료 경계, 개인정보·민감정보 동의 (동의 없이는 채팅 불가) |
| 채팅 | 메인 화면. 답변 스트리밍, 👍/👎 피드백, 상단 상시 "위기 시 도움" 링크 |
| 도움이 필요할 때 | 위기 연락처·안내 (정적 페이지, AI 미사용) |
| 내 정보/데이터 | 대화 삭제, 계정 삭제, 동의 관리 |
| (내부용) 운영 확인 | MVP에서는 별도 관리자 화면을 만들지 않고 Supabase 대시보드로 안전 로그를 확인 |

### 1-4. 코드 폴더 구조 (제안)

```
app/            화면과 API (Next.js)
lib/safety/     안전 라우터·위기 응답·출력 검사  ← 가장 중요, 테스트 필수
lib/rag/        검색·프롬프트 조립
lib/ai/         AI 호출을 감싸는 파일 (나중에 AI 회사를 바꾸기 쉽게 한 곳에 모음)
supabase/       DB 구조 변경 파일(migrations)
data/           Excel → CSV 변환본 (지식 원본)
scripts/        지식 import·임베딩 생성 스크립트
tests/          안전 시나리오 테스트
```

### 1-5. 기술 결정과 이유 (고정 스택 유지)

| 결정 | 선택 | 이유 |
|---|---|---|
| 프론트/서버 | Next.js (App Router, TypeScript) | 화면+서버 기능을 한 곳에서. Vercel과 궁합 최고 |
| DB/로그인 | Supabase (PostgreSQL) | 로그인·DB·벡터 검색을 한 서비스로 |
| 벡터 검색 | pgvector | chunk가 82개뿐이라 별도 벡터DB 불필요. 정확 검색으로 충분 (인덱스 최적화는 수천 개 이상일 때) |
| 호스팅 | Vercel | git에 올리면 자동 배포 |
| 리전 | Supabase·Vercel 모두 서울/아시아 | 한국 사용자 속도, 데이터 위치 명확화 |
| AI 회사 | **하나만 선택 (아래 결정 필요 #1)** | 키 1개, 청구서 1개로 단순화 |
| 요금제 | **운영은 유료 플랜 필요** | Vercel 무료(Hobby)는 상업적 이용 불가 약관, Supabase 무료는 비활성 시 프로젝트 일시 중지 및 백업 제한. 프로토타입 단계에서만 무료 사용 (최신 약관은 가입 시 재확인) |

---

## 2. Supabase Table 구조

세 그룹으로 나눕니다. 비유하면 **A=도서관 장서(읽기 전용), B=회원 개인 서랍(본인만), C=운영 기록(관리자만)**.

### A. 지식 테이블 (Excel에서 import, 브라우저 접근 불가·서버만 읽음)

| 테이블 | 주요 컬럼 (Excel 시트 컬럼 전부 포함) | 비고 |
|---|---|---|
| `system_prompt_sections` | section_order, section_id, name, prompt_text, priority | Critical 섹션은 항상 포함 |
| `safety_rules` | rule_id, category, trigger(text[]), rule_text, priority, handoff_action, notes | trigger를 `|`로 쪼개 배열로 저장 |
| `response_routes` | route_order, intent_or_trigger, precheck, retrieval_scope, top_k, required_policy(text[]), response_mode, human_handoff, program_route, forbidden | |
| `source_policy` | level, source_class, allowed_use, attribution_rule, medical_use, numerical_claims | |
| `articles` | article_id, title, author, source_type, evidence_status, primary_domain, … | 원문 전체는 저장하지 않아도 됨 (chunk 사용) |
| `knowledge_chunks` | chunk_id, article_id, chunk_text, **embedding vector(1536)**, domain_tags, issue_tags, wellness_theme, persona_tags, evidence_level, use_when, follow_up_prompt, ai_attribution_rule, **do_not, do_not_apply_when**, priority, **is_active** | `is_active`로 "Needs verification" 등을 검색에서 제외 |
| `frameworks`, `question_library` | 시트 그대로 (steps는 jsonb) | |
| `programs`, `program_routing_rules` | 시트 그대로 + `clinical_flag`, `doctor_referral_rule`, `do_not` | 통계 필드는 저장하되 노출 금지 |
| `service_knowledge`, `brand_knowledge` | 시트 그대로 | 서비스 문의 시 "공식 문구" 원천 |
| `app_config` | key, value (developer_config) | 튜닝값을 코드 수정 없이 변경 |
| `crisis_resources` (**신규**) | region, resource_name, contact, hours, verified_at, verified_by | Excel에 없는 항목. 검수자·확인일 기록 |
| `content_versions` (**신규**) | version, imported_at, imported_by, notes | 어떤 지식 버전으로 답했는지 추적 |

### B. 사용자 테이블 (Row Level Security: 본인 행만 접근)

| 테이블 | 주요 컬럼 | 비고 |
|---|---|---|
| `profiles` | user_id (auth 연동), display_name, created_at, birth_year(선택), locale | 최소 정보만 |
| `consents` | user_id, consent_type(service / sensitive_data / **marketing(선택, 기본 미동의)**), version, agreed_at, withdrawn_at | 동의 이력 보관 (버전별). 마케팅은 분리 동의, 철회 가능 |
| `wellness_profiles` | user_id, dessert_type, theme_scores(jsonb), source(self_report) | **개인화 전용. 진단 아님 표시.** 안전 라우터가 읽지 못하게 분리 |
| `chat_sessions` | session_id, user_id, started_at, safety_flag(none/elevated/crisis), topic_tag | `safety_flag`는 세션 내 상승만 가능 (아래 4장) |
| `chat_messages` | message_id, session_id, role, content, created_at, route, feedback(-1/0/1) | 민감 내용: 접근 통제 + 보관기간 정책 |
| `user_memory` | user_id, summary(≤1000자), updated_at | **MVP 이후.** 민감정보 최소화 |

### C. 운영/감사 테이블 (관리자·서버만)

| 테이블 | 주요 컬럼 | 비고 |
|---|---|---|
| `retrieval_logs` | request_id, message_id, selected_chunk_ids, similarity_scores, framework_id, content_version, model_name | 어떤 지식으로 답했는지 |
| `safety_events` | event_id, message_id, route, rule_ids(text[]), detection_layer(keyword/classifier/output_check), action(template/blocked/regenerated), created_at | 안전 감사 기록. **메시지 원문은 복제하지 않고 message_id로만 연결** |
| `handoff_events` | user_id, session_id, reason, offered_at, user_response | 반복 판매 방지(빈도 제한)용 |

### 보안 규칙 (테이블 공통)

- 모든 사용자 테이블에 **RLS 켜기** (`user_id = 로그인한 본인`).
- 지식 테이블은 브라우저용 키로는 읽을 수 없게 하고 서버 전용 키(service role)로만 접근. **서버 전용 키는 절대 화면 코드/깃에 넣지 않음.**
- 삭제 요청 시 대화·메모리 삭제 가능해야 함 (계정 삭제 연동).
- 로그(`safety_events`, `retrieval_logs`)에는 개인 식별·건강 정보를 남기지 않음. `retrieval_logs.query` 원문 저장은 **보관기간 정책 확정 후에만**.

---

## 3. AI Chat / RAG Architecture

### 3-1. 지식 넣기 (Ingest, 한 번 + 지식이 바뀔 때)

```
Excel → CSV로 저장 → 검증 스크립트(필수 컬럼, ID 중복, 태그 오타) 
      → DB 테이블에 적재 → knowledge_chunks.chunk_text만 임베딩 생성 → 저장
```
- 임베딩 = 글을 "의미 좌표(숫자 목록)"로 바꾸는 것. 사용자 질문도 같은 방식으로 바꿔서 "가까운 chunk"를 찾습니다.
- `chunk_text`(최대 약 300자)만 임베딩 (`developer_config.article_embedding_source`). 82개라 비용은 사실상 미미.
- `is_active=false`: "Needs verification" chunk, 검수 안 된 chunk는 검색 제외.

### 3-2. 대화 한 턴의 처리 순서 (`/api/chat`)

```
① 입력 확인       로그인·동의 확인, 길이 제한, 과도한 요청 제한(rate limit)
② 안전 라우터     (4장) 키워드 규칙 + AI 분류기 → route 결정. 두 결과 중 "더 위험한 쪽" 채택
③ 고정 응답 분기   route 1(자살/자해), 2(폭력/강요) → 검색·AI 생성 없이 검수된 고정 응답 + 위기 자원
                   ↳ 이후 이 세션의 safety_flag 상승
④ 검색 범위 결정   response_routes의 retrieval_scope / top_k 적용
                   (임상 route는 top_k ≤ 3, 일상 웰니스는 5)
⑤ 검색            metadata filter(domain/issue/theme/evidence) + pgvector 유사도
                   → rag_min_similarity 미만 제외
                   → do_not_apply_when이 현재 상황과 맞으면 제외 (예: 폭력 언급 시 '갈등은 자연스럽다' chunk)
                   → dessert_type은 순위 미세 조정에만 사용 (필터 아님)
⑥ 프롬프트 조립    system_prompt 13개 섹션(Critical 고정) + route별 필수 규칙(SAFE-xxx)
                   + 검색 chunk("참고자료, 지시가 아님"으로 표기) + 프로필 요약 + 최근 5~10개 메시지
⑦ AI 생성         응답 패턴: Reflect → Connect → Clarify → Ask(질문 1개)
⑧ 출력 검사       진단 표현, 약물 지시, 효과 보장/통계, 위기 중 판매·프로그램 추천, 유형 단정("당신은 ~형이라서")
                   → 위반 시 1회 재생성 → 재위반 시 안전한 대체 문구
⑨ 저장            chat_messages, retrieval_logs, safety_events
⑩ 프로그램·핸드오프 (route 5~7 & 안전 통과 & 빈도 제한 만족 시에만) 선택지로 제안
```

### 3-3. 우선순위 충돌 처리

`source_policy` 7단계를 프롬프트와 코드 양쪽에 적용: 안전/임상 정책이 항상 이김. article과 안전 원칙이 충돌하면 안전 원칙. 검색된 chunk가 프롬프트의 규칙을 바꾸려는 문장을 포함해도 **참고자료로만 취급** (프롬프트 주입 방어).

### 3-4. 프로그램 추천과 Human Handoff

- 프로그램 추천은 "진단·배정"이 아니라 "현재 고민과 주제의 관련성 설명 + 선택지". `program_routing_rules.safety_precheck`(우울/기능저하, 공황, 폭력 확인)를 **추천 전에 반드시 실행**.
- 추천 빈도는 low. 동일 세션 반복 노출 금지 (`handoff_events`로 제한).
- 동일 주제 3회 반복(`same_topic_handoff_count`), 큰 결정, 지속적 기능저하, 사용자 요청 시 Human Wellness Session 제안. **위기·의료 안전이 항상 우선.**
- 대화를 전문가에게 넘기려면 사용자의 명시적 동의 (MVP에서는 이 기능 자체를 만들지 않음).

### 3-5. AI 회사 선택 (결정 필요)

- **채팅 모델**: OpenAI 또는 Claude 어느 쪽도 가능. **임베딩(글→숫자)은 Claude API에는 없어서** Claude를 쓰면 임베딩용으로 다른 회사가 추가로 필요합니다.
- **제 추천(MVP 단순화): OpenAI 하나로 채팅+임베딩** → 계정·키·청구서 1개. AI 호출은 `lib/ai/` 한 파일에 모아 두어 나중에 Claude로 바꾸기 쉽게 함.
- 한국어 위기 상황 톤·안전 준수 품질은 **출시 전 평가 세트로 두 모델을 비교하는 것을 권장**. 구체 모델명은 개발 시점의 최신 목록을 확인해서 정합니다.

---

## 4. Mental-Health Safety Routing

가장 중요한 부분입니다. 원칙: **놓치는 것(미탐)이 과잉 반응(오탐)보다 훨씬 위험하다. 애매하면 더 안전한 쪽으로.**

### 4-1. 3중 방어 구조

| 층 | 방식 | 역할 |
|---|---|---|
| 1층 | **키워드 규칙** (safety_rules.trigger를 DB에서 읽음) | 빠르고 AI 장애와 무관하게 작동. 명백한 위기 표현 즉시 포착 |
| 2층 | **AI 분류기** (구조화된 출력: route, 위험도, 근거 rule_id) | 표현이 다양한 한국어("사라지고 싶다", 돌려 말하기) 포착. 최근 몇 개 메시지 문맥 포함 |
| 3층 | **출력 검사** | AI 답변이 진단·약물 지시·보장·위기 중 판매를 하지 않았는지 검사 |

1층과 2층 결과 중 **더 높은 위험도를 채택**합니다.

### 4-2. Route별 동작 (`response_routing` 그대로)

| 순서 | 상황 | 근거 | 동작 | 금지 |
|---|---|---|---|---|
| 1 | 자살/자해/구체적 위험 | SAFE-013 | **고정 위기 응답**: 지금 안전한지 확인, 혼자 있지 않기, 응급·위기 자원, 신뢰할 사람. 검색·프로그램·판매 없음 | 일반 article 분석, 판매, 방법·수단 정보 |
| 2 | 폭력/성적 강요/협박/스토킹 | SAFE-001 | 안전 확보 우선 + 지원 자원. 피해자 책임 암시 금지 | "갈등 조율" 프레임 |
| 3 | 진단/약물/정신질환 질문 | SAFE-003/007/008 | 경계 설명(진단·약물 지시 불가) + 의사·약사 상담 안내 + 웰니스 보조 | 진단, 약물 시작·중단·용량, 치료 우열 |
| 4 | 우울/공황/트라우마/기능저하 | SAFE-009/010/012 | 의료 평가 권유 후 자기이해 지원. 급성 흉통·호흡곤란은 의학적 평가 안내 | 원인 단정, 성장·창작 미화, 명상 강권 |
| 5 | 중요한 인생 결정 | SAFE-002 | 기준·감정·선택지 정리. 결정은 사용자에게 | 결정 대신 내리기 |
| 6 | 일상 웰니스/관계/창작 | SAFE-004/005 | 기본 RAG 응답 | 유형 단정, 검증 안 된 통계 |
| 7 | 서비스 문의 | SAFE-016 | 공식 서비스·브랜드 문구 기반 | 치료기관처럼 표현, 효과 보장 |

### 4-3. 세션 안전 상태 (safety_flag)

- 한 번 위기(route 1~2) 또는 임상 경계(3~4)가 감지되면 세션 `safety_flag`를 올리고, **이후 턴에서도 프로그램 추천·판매 톤 금지**.
- 세션 중 자동으로 낮추지 않음. 새 세션에서 재평가.
- 현재 메시지가 무해해 보여도 직전 위기 문맥이 있으면 이전 문맥을 함께 분류.

### 4-4. 알려진 어려움과 대응

| 어려움 | 대응 |
|---|---|
| "배고파 죽겠다", "웃겨 죽겠다" 같은 관용 표현 오탐 | 명시적 표현은 즉시 위기 응답, 모호한 표현은 2층이 문맥 판단 후 **부드러운 안부 확인 질문**으로 시작 |
| 표현 변형·은유 | 키워드 목록 확장 + 평가 세트 + 운영 로그에서 미탐 사례 반영 |
| 고정 위기 문구 품질 | **정신건강 전문가 검수 필수**, 검수자·날짜 기록 (`crisis_resources`) |
| 응답 지연/AI 장애 | 1층 + 고정 응답은 AI 없이 동작하므로 위기 대응은 항상 가능 |
| 미성년자 | MVP는 성인(만 19세 이상) 전용을 권장. 연령 정책은 법률 검토와 함께 확정 |

### 4-5. 출시 전 필수 게이트 (Launch Gate)

1. **안전 평가 세트** 작성 (예: 위기·폭력·진단·약물·오탐 유도 메시지 각 수십 개, 한국어) — 전문가 검수
2. 위기 시나리오 통과율 목표: **미탐 0건**을 목표로 기준 확정 (기준 수치는 전문가와 함께 결정)
3. 출력 검사 우회 시도 테스트 (프롬프트 주입, "이전 지시 무시해" 등)
4. 위기 연락처 최신성 재확인
5. 개인정보·민감정보 동의 문구 법률 검토
6. 이 게이트를 통과하기 전에는 **실사용자에게 공개하지 않음** (내부·지인 테스트만)

---

## 5. MVP에서 먼저 구현할 기능

원칙: **Excel README의 권장 순서**(안전·프롬프트 → ingest → 라우터 → 프로필·메모리 → 프로그램·핸드오프 → 평가·로그)를 따르되, 메모리·프로그램 추천은 MVP 뒤로 미룹니다.

각 단계는 **한 번에 한 기능**만 만들고, 단계마다 (1) 쉬운 설명 → (2) 체크포인트 → (3) 해당 기능만 구현 → (4) 테스트 → (5) 사용자가 직접 할 클릭·설정 안내 → (6) 무관한 기능은 건드리지 않음.

| 단계 | 기능 | 완료 기준 | 사용자가 밖에서 해야 할 일 (예상) |
|---|---|---|---|
| **M0** | 프로젝트 뼈대: Next.js 생성, git 저장소, Vercel 연결(빈 화면 배포), `CLAUDE.md` 확정 | 배포된 빈 페이지가 URL로 열림 | GitHub·Vercel·Supabase 계정 생성 (비밀번호·결제는 본인이 직접) |
| **M1** | Supabase 연결 + 로그인 + 동의 화면 + RLS | 가입·로그인·동의 기록, 타인 데이터 접근 불가 확인 | Supabase 프로젝트 생성(서울 리전), 키를 Vercel 환경변수에 등록 |
| **M2** | 지식 테이블 생성 + Excel import + 검증 스크립트 | 17개 시트 중 필요한 것 적재, 행 수 일치(chunk 82개 등) | Excel을 CSV로 저장해 제공 |
| **M3** | 임베딩 생성 + 검색 테스트 (채팅 없이) | 샘플 질문 → 관련 chunk 반환, do_not_apply_when 제외 동작 | AI API 키 발급·환경변수 등록, 사용 한도 설정 |
| **M4** | **안전 라우터 + 고정 위기 응답 + 위기 페이지** (AI 답변 생성 이전에 먼저) | 안전 평가 세트 통과, 위기 응답에 검색·판매 없음 | **위기 문구·연락처 검수자 지정 및 승인** |
| **M5** | 채팅 화면 + `/api/chat` (프롬프트 조립, 생성, 출력 검사, 스트리밍) | 6개 route 모두 예상대로 응답, 로그 저장 | 테스트 대화 직접 해보기 |
| **M6** | 피드백(👍/👎) + `retrieval_logs`/`safety_events` + 대화·계정 삭제 | Supabase 대시보드에서 로그 확인, 삭제 동작 | 로그 보관기간 결정 |
| **M7** | 웰니스 프로필(디저트 유형 자가 선택) — **재랭킹/표현 전용** | 유형이 검색 필터·안전 판단에 쓰이지 않음을 테스트로 증명 | (선택) 유형 소개 문구 확정 |
| **M8** | 폐쇄 베타 준비: Launch Gate 점검, 오류 모니터링, 사용량·비용 상한 | 4-5장 게이트 전 항목 통과 | 법률 검토 결과 반영, 베타 참가자 선정 |

> M4를 M5보다 **먼저** 만드는 이유: 안전 장치 없이 AI 채팅이 먼저 열려 있는 상태를 만들지 않기 위해서입니다.

---

## 6. 나중에 구현할 기능 (MVP 이후)

| 우선 | 기능 | 메모 |
|---|---|---|
| 1 | 장기 메모리 요약 (`user_memory`, 500~1000자) | 민감정보 최소화, 사용자가 보고 지울 수 있어야 함 |
| 2 | 프로그램 추천 (`program_routing_rules` + 사전 안전 확인) | 통계·효과 수치 검증 전 노출 금지 |
| 3 | Human Wellness Session 안내·예약 연결 | 처음엔 예약 페이지 링크, 이후 앱 내 예약 |
| 4 | 전문가와 대화 공유 | **명시적 동의** 필수, 기본 자동 공유 금지 |
| 5 | 앱 내 웰니스 테스트 (디저트 유형 산출) | 진단 아님 표기, 개인화 전용 |
| 6 | 운영자 관리 화면 (지식 편집·검수 상태, 안전 로그, 피드백) | MVP는 Supabase 대시보드로 대체 |
| 7 | 평가 대시보드·자동 회귀 테스트 | 모델 변경 시 안전 세트 자동 재검증 |
| 8 | 소셜 로그인 (카카오 등) | 가입 장벽 감소 |
| 9 | English 서비스 (SERVICE-ENG-001) | 영어용 위기 자원·지역별 안내 필요, 카피 수정 후 |
| 10 | 새 article 추가 파이프라인, 임베딩 재생성 | `content_versions`로 이력 관리 |
| 11 | 결제 | 별도 법·정책 검토 |
| 12 | 모바일 앱/PWA, 알림 | 웹 안정화 후 |

---

## 7. 결정 사항

### 7-1. 확정된 결정 (2026-09-21, 사용자 확정)

| 항목 | 결정 | 계획서 반영 |
|---|---|---|
| AI 회사 | **OpenAI 하나**로 채팅+임베딩. 호출은 `lib/ai/` 한 곳에 모음 | 3-5장. 출시 전 모델 평가는 여전히 권장 |
| 서비스 대상 | **한국어 · 한국 거주자 · 성인(만 19세 이상)** | MVP는 가입 시 "만 19세 이상, 한국 거주" 자기 확인 + 출생연도 입력. 해외·영어는 나중(6장 9번) |
| 위기 문구 승인 | **시스템 관리자가 검수·승인.** 강한 위기 문구는 정신상담 전문가·관련 기관에 연결 | `crisis_resources.verified_by`에 승인자 기록. 아래 7-2 참고 |
| 개인정보 | **개인정보보호법에 따름.** 서비스 운영 목적에만 사용. **마케팅은 별도 동의한 사용자에게만** | `consents`에 `marketing`(선택) 유형을 서비스·민감정보 동의와 **분리**. 기본값은 미동의 |
| 요금제 | Vercel·Supabase **유료 플랜 가입 완료** | M0 이후 바로 운영용 설정 가능 |

### 7-2. 위기 응답 문구 (사용자 제공 원문, `crisis_resources` 초기 데이터)

> 우울감 등 말하기 어려운 고민이 있거나 주변에 이런 어려움을 겪는 가족·지인이 있을 경우, 자살예방 상담전화 ☎109 또는 정신건강 상담전화 ☎1577-0199에서 24시간 전문가의 상담을 받을 수 있습니다.

- 이 문구는 **안내 문구(도움 자원)**로 쓰고, 위기 페이지와 모든 위기 응답에 포함합니다.
- 번호는 사용자가 제공한 것이며, 제가 공식 출처로 직접 확인하지는 못했습니다. 관리자가 M4 단계에서 공식 기관 사이트로 **최종 확인 후 확인일을 기록**합니다.
- 이 문구만으로는 **즉각 위험(route 1)** 응답으로 부족합니다. 지금 안전한지 묻는 질문, 혼자 있지 않기, 응급 상황 안내가 필요합니다. 아래는 제 제안이며 **승인 전에는 적용하지 않습니다**:
  1. 안전 확인 질문 ("지금 안전한 곳에 계신가요?")
  2. 즉각적 위험 시 **119(응급)** 또는 가까운 응급실 안내
  3. 곁에 있을 수 있는 신뢰할 사람에게 연락 권유
  4. 위 109 / 1577-0199 안내

  → **사용자 승인 완료 (2026-09-21).** 4요소 구성으로 route 1의 고정 응답 초안을 M4에서 작성합니다. 실제 문장은 관리자가 최종 확인하고, 전문가 검토는 Launch Gate에 유지합니다.
- 강한 위기 문구(route 1·2)는 출시 전 **정신건강 전문가 또는 관련 기관의 검토를 받는 것**을 Launch Gate(4-5장)에 유지합니다. 관리자 승인만으로 실사용자에게 공개하지 않습니다.

### 7-3. 추가 확정 (2026-09-21)

| 항목 | 결정 | 계획서 반영 |
|---|---|---|
| 즉각 위험 문구 | 4요소 구성 승인 | 7-2 |
| AI 사용료 상한 | **1인당 월 3,000원**을 넘으면 제한 | 아래 7-4 |
| 디저트 유형 | **기존 테스트 결과를 가져오고, 필요하면 재테스트** | `wellness_profiles.source`(imported/retest), `tested_at` 추가. 여전히 개인화 전용. 가져오는 방식은 7-5 질문 참고 |
| 도메인 | 아직 없음 | 개발·테스트는 Vercel 기본 주소(`*.vercel.app`) 사용. 베타 전 도메인 구입(구입·결제는 본인이 직접) |
| 로고 | 첨부 이미지 사용 (남색 파도 심볼 + "쏠 웰니스 하우스") | M0에서 사이트에 배치. 브랜드 색은 로고의 남색을 기준으로 삼음 |

### 7-4. 보관기간: 무엇에 어떤 규칙이 적용되는지 정리

제공해 주신 기간은 **종류별로 다른 규칙**이라, 대화 내용과 섞이지 않게 나눠서 반영합니다.

| 종류 | 기간(제공해 주신 내용) | MVP에 해당? | 반영 |
|---|---|---|---|
| 결제·계약·청약철회 기록 | 5년 | **아니오** (결제 기능은 6장 11번, MVP 이후) | 결제 기능을 만들 때 `payment_records`에 적용 |
| 소비자 불만·분쟁 기록 | 3년 | 아니오 (결제와 함께) | 동일 |
| 로그인·접속 기록(IP 등) | 3개월 | 예 | 서버 접속 로그 보관 3개월 후 자동 삭제 설정 |
| 개인정보 처리시스템 접속기록(관리자가 회원 정보 열람한 기록) | 2년 이상 (민감정보 처리 시) | 예 | 관리자 열람 감사 로그(`admin_access_logs`) 신설, 2년 이상 보관 |
| **AI 대화 내용** | **아직 미정** | 예 | 아래 제안 참고 |

- 위 기간은 제공해 주신 자료(대학·기관 개인정보 안내 페이지)를 그대로 옮긴 것이며, 제가 법령 원문으로 확인하지는 못했습니다. **처리방침 확정 전에 법률 전문가 또는 개인정보 담당자의 확인을 받아 주세요.**
- **중요:** 전자상거래법의 5년/3년은 **결제·계약·분쟁 기록**에 대한 것이지, 정신건강 관련 **대화 내용**을 5년 보관하라는 뜻이 아닙니다. 민감정보인 대화 내용은 **오래 보관할수록 위험**하므로 별도 기준이 필요합니다.
- **대화 내용 보관 제안 (승인 필요, 제 의견):**
  1. 사용자가 삭제하면 즉시 삭제, 탈퇴 시 함께 삭제
  2. 마지막 이용 후 일정 기간(예: 12개월) 지나면 자동 삭제
  3. 안전 감사 기록(`safety_events`)에는 대화 원문을 넣지 않음 (이미 설계됨)
  4. 원문을 평가·개선에 쓰려면 **별도 동의**를 받거나 비식별 처리
  - 기간 숫자는 법률 확인 후 확정합니다.

### 7-5. AI 사용료 상한 (1인당 월 3,000원)

**적절합니다.** 다만 아래 보완을 넣는 것을 제안합니다.

- 비용은 모델에 따라 크게 다릅니다. 작은 모델이면 3,000원으로 수백 번 대화할 수 있고, 큰 모델이면 수십 번 수준일 수 있습니다. **M5에서 실제 모델 가격을 확인한 뒤** 몇 번 대화가 가능한지 계산해서 보여드리겠습니다.
- 보완 제안:
  1. **위기 대응은 상한과 무관하게 항상 응답합니다.** 고정 문구는 AI 비용이 들지 않고, 사용량 제한 때문에 도움이 막히면 안 됩니다. (안전 규칙 최우선)
  2. 상한의 80%에 가까워지면 미리 안내하고, 초과 시 "이번 달 사용량을 모두 사용했어요. 다음 달에 다시 이용하거나 Human Wellness Session을 이용할 수 있어요" 같은 친절한 안내를 표시. 초과 안내에도 진단·압박 표현 금지.
  3. **원화가 아니라 토큰(AI 사용 단위)으로 계산**하고 환율 변동은 설정값으로 관리 (`app_config`에 저장, 코드 수정 없이 변경).
  4. 분류기·임베딩 호출 비용도 포함하되, 안전 분류 호출은 상한 초과 후에도 유지.
  5. **전체 월 상한**(서비스 전체 예산)도 함께 설정 + OpenAI 계정 자체의 월 한도 설정(마지막 안전장치). 이 설정은 **OpenAI 사이트에서 직접** 하셔야 합니다.

### 7-6. (이전 목록 — 최신 상태는 7-7 참고)

1. **대화 내용 보관기간 숫자** — 7-4 제안(12개월 자동 삭제 등)을 승인하거나 수정
2. **국외 이전 고지** — OpenAI 등 해외 서버 사용 시 처리방침 고지 문구 (법률 확인)
3. **한국 거주 확인 방식** — MVP는 자기 확인(체크박스)으로 충분한지
4. **전체 서비스 월 AI 예산 상한**
5. **기존 디저트 테스트 결과를 어디서 어떻게 가져오는가** — (a) 어느 시스템/파일에 있는지 (b) 결과 형식 (c) 이 앱 회원과 연결하는 방법(예: 같은 이메일). 결과를 가져오는 것 자체도 사용자 동의 항목에 포함합니다.
6. 로고 파일: 지금 이미지는 흰 배경 JPG입니다. 웹에 잘 쓰려면 **투명 배경 PNG 또는 SVG**, 브라우저 탭 아이콘용 **정사각형 파도 심볼**이 있으면 좋습니다. 없으면 현재 이미지로 시작합니다.

---

### 7-7. 최신 결정 및 남은 사항 (2026-09-21)

**확정**
| 항목 | 결정 |
|---|---|
| 대화 내용 보관 | **승인**: 삭제/탈퇴 시 즉시 삭제, 마지막 이용 후 12개월 자동 삭제, 원문 활용은 별도 동의 또는 비식별 |
| 한국 거주 확인 | MVP는 체크박스 자기 확인 |
| 국외 이전 고지 | 처리방침·동의 화면에 넣음 (초안은 부록 A. 법률 검토 후 확정) |
| 로고 | 현재 JPG로 시작 |
| 동의 유형 추가 | `overseas_transfer`(국외 이전), `dessert_import`(디저트 결과 가져오기) — 서비스·민감정보·마케팅과 분리 |

**계정(아이디/비밀번호) 공유 — 조사 결과와 제안**

- 아임웹은 네이버·카카오·구글 등 **소셜 로그인**과, 관리자용 **API 키로 회원 데이터를 연동**하는 기능이 있습니다. 반면 제가 찾은 범위에서는 **"아임웹 아이디로 외부 앱에 로그인"하는 공식 방식(SSO)은 확인하지 못했습니다.** 확정하려면 아임웹 고객센터에 문의가 필요합니다.
- 설령 가능해 보여도 **비밀번호를 두 시스템이 공유하는 것은 권하지 않습니다.** 비밀번호는 원문이 아닌 암호화된 값으로만 저장되어 옮길 수 없고, 옮기려 하면 보안·개인정보 사고 위험이 큽니다. 이 앱은 **Supabase Auth가 자체 비밀번호를 관리**합니다.
- 따라서 "같은 아이디·비밀번호로 두 곳 로그인"은 **MVP에서 목표로 두지 않고**, 아래 방식을 제안합니다.

| 방식 | 내용 | 시점 |
|---|---|---|
| **A. 앱에서 별도 가입 + 이메일로 연결 (추천)** | 앱에서 이메일로 가입(이메일 인증). 아임웹 회원과는 **같은 이메일**로 사용자가 동의할 때만 연결 | MVP |
| **B. 카카오/네이버 소셜 로그인 공통 사용** | 아임웹과 앱 모두 같은 소셜 계정으로 로그인하면 사실상 같은 사람으로 인식. 비밀번호를 옮길 필요 없음 | MVP 직후 |
| C. 진짜 통합 로그인(SSO) | 한 곳을 로그인 기준으로 삼는 구조. 개발·운영 부담 큼 | 나중 (아임웹 제약 확인 후) |

**도메인: 신규 도메인이 꼭 필요한가?**
- **새로 사야 하는 것은 아닙니다.** 이미 도메인을 갖고 계신다면 `app.도메인.kr` 같은 **하위 도메인**을 Vercel에 연결하면 됩니다. 아임웹 사이트는 그대로 둡니다. (아임웹 FAQ에 2차 도메인을 다른 서비스에 연결하는 방법이 있으며, 아임웹에서 구매한 도메인은 설정 변경에 아임웹 고객센터 요청이 필요할 수 있음)
- 하위 도메인으로 연결해도 **아임웹 로그인 상태가 앱과 자동으로 공유되지는 않습니다.** 위 A/B 방식이 필요한 이유입니다.
- 도메인이 있으면 이메일 인증 메일을 브랜드 주소로 보낼 수 있어(가입 메일 신뢰도) 도메인 자체는 베타 전 꼭 필요합니다.
- **제가 확인해야 할 것**: 현재 아임웹 사이트 주소(도메인)와 구매처(아임웹/가비아 등). 이를 알려주시면 하위 도메인 연결 가능 여부와 준비할 것을 안내하겠습니다.

**디저트 테스트 결과 가져오기**
- 결과가 웹페이지에 있다는 것까지 확인했습니다. 다음이 필요합니다: (1) 그 테스트가 **아임웹 페이지인지, 별도 테스트 도구(구글폼, 타입폼 등)인지**, (2) 결과가 **저장되어 내보낼 수 있는지**(엑셀·API), 아니면 사용자 화면에만 표시되는지.
- 연동이 어려우면 MVP 대안: **사용자가 자기 유형을 직접 선택**(`source=self_select`) + "다시 테스트하기" 링크 안내. 이 방식은 시스템 연동이 필요 없고 개인화 전용 원칙과도 충돌하지 않습니다.
- 가져온 값이든 직접 선택이든 **진단이 아니며 안전 판단에 쓰이지 않음**은 동일합니다.

**AI 사용료 (시뮬레이션 후 결정)**
- 1인당 월 3,000원 상한은 확정. "회원당 10,000원"은 **서비스 전체 예산을 잡기 위한 값**으로 이해했습니다(예: 회원 N명 × 예상 이용률). 의도와 다르면 알려주세요.
- M5에서 실제 모델 가격을 확인한 뒤 시뮬레이션을 만듭니다. 변수: 회원 수, 월 활성 비율, 1인 월 평균 대화 수, 1회 대화의 토큰, 모델 종류, 환율. 결과표를 보고 전체 상한을 정합니다.

**아직 남은 것**
1. 현재 아임웹 사이트 도메인·구매처
2. 디저트 테스트의 위치·형식 (위)
3. 법률 검토 항목: 국외 이전 고지 문구, 보관기간·접속기록 기간 숫자, 동의서 문구
4. 전체 월 AI 예산 (시뮬레이션 후)

### 7-8. 사이트 확인 결과와 확정 사항 (2026-09-21)

**확정**
- AI 예산 의도 확인됨 (1인당 월 3,000원 상한 + 전체 예산은 M5 시뮬레이션 후 결정).
- 법률 검토는 사용자가 받기로 함 (베타 공개 전까지).
- **계정·DB 분리 확정**: 아임웹 계정(아이디/비밀번호)과 앱 계정은 **별개**이며 각자 데이터베이스로 관리. 앱 계정은 Supabase Auth. 앱의 DB에 아임웹 회원의 비밀번호나 개인정보를 복사하지 않음. 연결이 필요하면 7-7의 A안(같은 이메일, 사용자 동의 시)만 사용.

**`ssolwellness.com` 확인 결과** (공개 페이지 확인 범위)
- 아임웹으로 운영 중 (푸터에 "Hosting by 아임웹").
- 도메인 등록처는 가비아이고, 네임서버는 `hostcocoa.com`으로 확인됨 → **아임웹이 DNS를 관리하는 구성으로 보임** (아임웹 안내 문서 기준 추정, 아임웹 관리 화면에서 확인 필요).
- 아임웹 안내에 따르면 네임서버가 아임웹인 상태에서는 하위 도메인을 다른 서버로 직접 지정할 수 없고, **아임웹 고객센터(채팅)에 요청**해야 함. 따라서 **새 도메인 구입 없이 `app.ssolwellness.com` 같은 하위 도메인**으로 앱을 연결하는 방향을 우선 시도.
  - 방법 1: 아임웹 고객센터에 "app 하위 도메인을 Vercel로 연결" 요청 (Vercel이 알려주는 DNS 값 전달)
  - 방법 2: 네임서버를 가비아로 옮기고 아임웹용 레코드를 다시 설정 (사이트 중단 위험이 있어 **비추천, 아임웹 확인 후에만**)
  - 방법 3: 앱 전용 새 도메인 구입 (가장 단순하고 기존 사이트에 영향 없음)
- 사이트 메뉴에 **심리 테스트**(`/test`), **예약**(외부 예약 서비스 `epipro`) 존재. `/test`는 제 확인 시 홈으로 이동해 내용을 보지 못했음 → 테스트의 실제 위치·결과 저장 방식은 사용자 확인 필요.

**심리 테스트 결정 (사용자 확정)**: 테스트는 이 앱과 같은 Next.js 프로젝트 안에 **새로 만든다.** 따라서 아임웹에서 결과를 가져오는 연동은 필요 없음(7-3의 "기존 결과 가져오기"는 대체됨). 영향: `wellness_profiles.source`에 `app_test` 값 추가 필요(다음 마이그레이션), 테스트 문항·채점표·결과 문구는 사용자가 제공, 결과 문구는 진단 표현 금지·개인화 전용 원칙 적용. 비회원 응시 허용 여부는 미정.

**앱 이름/주소 제안**: 테스트 단계는 `*.vercel.app`, 베타는 `app.ssolwellness.com`(방법 1) 또는 새 도메인(방법 3).

### 7-9. 심리 테스트 프로토타입 분석 (`ssol-wellness-quiz.html`)

**프로토타입이 보여주는 실제 사용자 흐름**
`인트로 → 이름·호칭(공주/왕자/공작) → 20문항(5점 척도) → 결과(유형·레이더 차트·특징·베스트/상극 유형) → 공유 / 결과 저장(가입) / 심층 리포트(3,500원 결제 + 1주일 AI 채팅권)`

→ 테스트는 단독 기능이 아니라 **가입·결제·AI 채팅으로 이어지는 유입 퍼널**입니다. 이 때문에 계획에 아래 변화가 생깁니다.

| 프로토타입 요소 | 계획에 미치는 영향 |
|---|---|
| 비회원 응시 가능, 이후 "결과 저장하기"로 가입 | **비회원 응시 허용 확정으로 간주.** 서버에 익명 결과를 저장하지 않고 **브라우저에만 임시 보관 → 가입 직후 계정에 저장**(개인정보 최소화). 가입 전 서버 저장이 필요하면 별도 결정 |
| 3,500원 결제 + 1주일 AI 채팅권 | **결제가 "나중"에서 앞당겨짐.** MVP 뒤 별도 단계(P1)로 분리하되, 유료 출시 전에 필수. 전자상거래법 기록 보관(결제 5년·분쟁 3년, 7-4장)과 결제 대행사(PG) 연동, 청약철회·환불 정책, 이용약관 필요 |
| 가입: 카카오 / 네이버 / 이메일 / 휴대폰 인증 | Supabase는 **카카오 로그인을 기본 지원**, **네이버는 기본 목록에 없어 추가 작업 필요**(개발 시점에 재확인). 휴대폰 인증(SMS)은 발송 업체 별도 계약·비용 필요. **MVP: 이메일 + 카카오, 나중: 네이버·휴대폰** 제안 |
| "🍰 벌써 1,000명 넘게 참여했어요" | 실제 수치가 아니면 사용하지 않기. 프로덕션에서는 **실제 집계값을 쓰거나 삭제** (SAFE-016 취지: 검증되지 않은 수치 노출 금지) |
| 이미지 22개가 HTML에 통째로 포함 (2.6MB) | 이식 시 **파일로 분리**하고 최적화 (로딩 속도) |
| Google Fonts 외부 로드 | Next.js에서 **서비스 서버에서 직접 제공**(개인정보·속도 이점) |
| 공유(카카오/네이버/X 등) | MVP는 **링크 복사**만, 카카오 공유 SDK는 나중 |

**유형 ↔ DB 코드 매핑 (스키마의 `dessert_type`와 일치 확인 완료)**

| 프로토타입 키 | 유형 | DB 코드 |
|---|---|---|
| relate_F / relate_E | 딸기마카롱 / 슈크림빵 | strawberry_macaron / cream_bread |
| worth_F / worth_E | 밀푀유 / 머랭쿠키 | millefeuille / meringue_cookie |
| control_F / control_E | 티라미수 / 카라멜푸딩 | tiramisu / caramel_pudding |
| happy_F / happy_E | 요거트파르페 / 에스프레소브라우니 | yogurt_parfait / espresso_brownie |
| meaning_F / meaning_E | 피스타치오크루아상 / 카스텔라 | pistachio_croissant / castella |

**이식 원칙**: 문항 20개, 도메인, 리커트 라벨, 호칭, 10개 유형(이름·한 줄 성향·본문·특징 5개)은 **한 글자도 바꾸지 않습니다.** 손으로 옮기다 생기는 오타를 막기 위해 **프로토타입 파일에서 스크립트로 자동 추출**하고, 추출 결과와 원본이 일치하는지 테스트합니다.

**채점 방식 (프로토타입 코드에서 확인)**
1. 5개 영역별 점수 = 해당 영역 4문항(F 2개 + E 2개)의 응답값(1~5) 합. 미응답은 3점 처리
2. 점수가 가장 높은 영역이 주 영역
3. 주 영역 안에서 F 합계 vs E 합계가 큰 쪽이 성향(F/E) → 유형 10종 중 1개
4. 베스트/상극 유형: 영역 순환표(통제→관계→자기가치→의미→행복)로 계산

**확인이 필요한 점 (제가 임의로 바꾸지 않습니다)**
- **동점 처리가 무작위(`Math.random`)입니다.** 같은 답변으로 다시 해도 결과가 달라질 수 있고, 저장된 결과와 재현이 안 됩니다. 프로덕션에서는 **정해진 규칙**(예: 영역 순서 우선 또는 마지막 응답 기준 등)이 필요합니다 → 사용자 결정 필요.
- 문항 응답 원문은 심리 정보이므로, **점수·유형만 저장하고 문항별 응답은 저장하지 않는 것**을 제안합니다 (재계산이 필요하면 별도 결정).

**안전·표현 점검 (문구는 바꾸지 않고, 앱이 지켜야 할 것)**
- 유형 문구 자체에는 진단 표현이 없습니다. 다만 "당신은 ~한 사람입니다" 식의 확정적 어조와 "상극" 궁합 표현이 있으므로, 결과 화면에 **"자기이해를 위한 참고용이며 진단이나 의학적 평가가 아닙니다"** 안내를 추가할 것을 제안합니다 (기존 문구 수정이 아닌 **추가**).
- 결과 화면에도 **"힘든 마음이 지속되면 전문가 상담"과 위기 안내 링크**를 상시 노출 (테스트 응시자도 포함).
- 유형은 AI 채팅에서 **재랭킹·표현 개인화에만** 사용하고, "당신은 이 유형이라서 그렇다"는 표현 금지 (SAFE-005, 이미 반영).
- **심층 리포트**("왜 이 패턴이 나타나는지", "그동안 어떻게 살아왔을까" 등)는 응답 근거의 원인 해석·과거 추측이 진단처럼 읽힐 수 있는 영역입니다. **MVP에서는 제외**하고, 만들 때는 사전 작성 문구를 쓰거나 AI 생성 시 안전 규칙(SAFE-003/005/009)과 출력 검사를 통과시키며, 문구는 전문가 검수를 받는 것을 권합니다.

**수익 구조 점검 (계산만 제시, 결정은 사용자)**
- 3,500원에는 부가세와 결제 수수료가 포함되어 실수령이 대략 3,000원 안팎일 수 있습니다(세율·수수료율은 계약 확인 필요). 그런데 **1인당 월 AI 상한이 3,000원**이므로, 채팅권 1건(1주일)의 AI 상한은 **그보다 훨씬 낮게** 잡아야 마진이 남습니다. M5의 AI 비용 시뮬레이션에서 "채팅권 1주일 = 최대 N회 대화" 형태로 함께 계산하겠습니다.
- 채팅권 유효기간 1주일이 지나면 채팅 접근이 어떻게 되는지(무료 체험 여부, 위기 안내는 항상 열림)도 정해야 합니다.

**단계 변경 제안**
- **M7a**: 테스트 화면 이식(비회원 응시, 결과 표시, 링크 복사, 안내 문구) — 저장 없음
- **M7b**: 가입 후 결과 저장 (`wellness_profiles`, `source='app_test'`)
- **P1 (MVP 이후)**: 결제(PG), 채팅권, 심층 리포트, 카카오 공유·네이버 로그인

**DB 영향 (다음 마이그레이션에 포함할 것)**: `wellness_profiles.source`에 `app_test` 추가, `wellness_profiles.domain_scores jsonb`(5개 영역 점수 — 기존 `theme_scores` 사용), 호칭(`display_title`: 공주/왕자/공작)은 표시용으로만 `profiles`에 선택 저장.

### 7-10. 테스트·채팅권 결정과 지식 import (2026-09-21)

**확정**
| 항목 | 결정 |
|---|---|
| 채점 동점 | 최고점 영역이 동점이면 **영역 순서 우선**: 관계 → 자기가치 → 통제 → 행복 → 의미 (프로토타입의 `DOMAIN_ORDER`). 무작위 사용 안 함 |
| 채팅권 | 1주일권. **하루 5회 또는 10회** 제한(M5 시뮬레이션 후 결정). 월 AI 상한 3,000원 안에서 운영 |
| 채팅권 만료 후 | 채팅 시 결제 필요 안내 + **1개월권 결제 유도**. 위기 안내는 항상 열림 |
| 테스트 응답 저장 | **점수·유형만 저장, 문항별 응답은 저장 안 함.** 비회원 응시는 서버 추적 없음, "결과 저장하기" 시 가입 후 저장 |

**아직 남음**
- 주 영역 안에서 **F(직면)와 E(회피) 합계가 같을 때**의 규칙 (프로토타입은 무작위). 제안: **F 우선**. → 사용자 확인 필요
- 1개월권 가격, 하루 5회/10회 중 선택 (M5 시뮬레이션)

**지식 데이터 import 방식 (일회성, 재실행 가능)**
- `scripts/build_import_sql.pl`이 Excel을 읽어 upsert(있으면 갱신, 없으면 추가) SQL 파일을 생성. **API 키·설치 불필요**, 생성된 SQL을 Supabase SQL Editor에 붙여넣기.
- 필요 테이블은 `migrations/…000100_knowledge_tables.sql`로 먼저 생성.
- 기본 안전 처리: (1) "Needs verification" chunk, (2) 글 상태가 "Needs clinical …"인 글(ADHD·우울·공황)의 chunk 15개는 **비활성(is_active=false)으로 import** — 임상 검토 후 활성화. (3) chunk 본문이 바뀌면 기존 임베딩을 비워 재생성 대상으로 표시.

### 7-11. import 완료 및 후속 결정 (2026-09-21)

- **import 성공 확인됨** (사용자가 `import_check.sql`로 행 수 확인).
- **F/E 동점 규칙 승인**: 주 영역 안에서 F(직면)와 E(회피) 합계가 같으면 **F 우선**. 채점 규칙 최종: ① 영역 점수 최고 → 동점이면 관계→자기가치→통제→행복→의미 순 ② 주 영역 F 합 vs E 합 → 동점이면 F.
- **임상 chunk 15개 활성화 결정** (ADHD·우울·공황): 사용자 결정. 조건 — **이 chunk를 사용한 답변에는 항상 전문가 상담 안내를 함께 한다** (SAFE-003/007/009/010/011). 구현 규칙:
  1. 이 3개 글의 chunk는 route 3·4(임상 경계)에서만 검색, 일반 웰니스 route(6)에서는 제외
  2. 답변에 이 chunk가 쓰이면 출력 검사가 "전문가 상담 안내 문구 포함"을 확인하고, 없으면 재생성/고정 문구 추가
  3. 진단·원인 단정·약물 언급 금지는 그대로 (SAFE-003/008)
  4. 화면에 "의료 상담을 대체하지 않음" 상시 표시
  - 적용은 `supabase/import/activate_clinical_chunks.sql` (사용자가 SQL Editor에서 실행)
- **`SSOL-SELF-006`·`SSOL-SELF-004`(활성 chunk)**: 사용 결정. `SELF004-A`는 chunk 자체가 "Needs verification"이라 **계속 비활성** (별도 지시 시 활성화).
- **임베딩 방식 확인**: M3에서 OpenAI 키로 생성.
- import 스크립트는 위 결정을 반영해 수정됨 (임상 글 chunk는 새로 import해도 활성).

### 부록 A. 국외 이전 고지 문구 (초안 — 법률 검토 전)

> **개인정보의 국외 이전 안내**
> 쏠 웰니스 하우스는 AI 대화 서비스 제공을 위해 아래와 같이 개인정보를 국외로 이전(처리 위탁 포함)합니다.
>
> - **이전받는 자 / 국가**: [OpenAI, L.L.C. 등 정확한 법인명 확인] / 미국
> - **이전 항목**: 이용자가 AI 대화에 입력한 메시지 내용(건강·정서 관련 내용이 포함될 수 있음). 이름·이메일 등 계정 정보는 AI 회사에 전달하지 않도록 설계함
> - **이전 시기·방법**: 대화 메시지 전송 시 암호화된 통신망을 통해 실시간 전송
> - **이용 목적**: AI 답변 생성 및 검색용 데이터 처리
> - **보유·이용 기간**: [AI 회사 보관기간 확인 필요 — 예: 오남용 모니터링 목적 일정 기간 후 삭제]
> - **연락처**: [이전받는 자의 개인정보 관련 연락처 / 쏠 웰니스 하우스 개인정보 담당자]
> - **거부 방법 및 불이익**: 국외 이전에 동의하지 않으실 수 있으나, 이 경우 AI 대화 기능을 이용하실 수 없습니다.
>
> 데이터 저장·호스팅: 회원 정보와 대화 기록은 [Supabase 서울 리전 등 실제 설정 리전 기재]에 저장되며, 서비스 제공사(Supabase, Vercel)는 [국가]에 본사를 둔 사업자입니다. [위탁/이전 해당 여부 법률 확인]

- 대괄호 항목은 **실제 계약·설정을 확인해 채워야** 합니다 (특히 OpenAI의 API 데이터 보관·학습 사용 정책은 제가 최신 상태를 확인하지 못했으므로 공식 문서로 확인 필요).
- 동의 화면에서는 **`overseas_transfer`를 별도 체크박스**로 받고, 미동의 시 채팅을 막습니다 (위기 안내 페이지는 동의 없이도 항상 열람 가능).
- 최종 문구는 **법률 전문가 확인 후 확정**하며, 그 전에는 실사용자에게 배포하지 않습니다.

---

## 8. 작업 규칙 (실제 개발 시 매 단계 적용)

1. 무엇을 바꾸는지 쉬운 말로 먼저 설명
2. 체크포인트(git 커밋) 생성 — 문제가 생기면 이 지점으로 되돌릴 수 있음
3. 해당 기능만 구현
4. 관련 테스트 실행 후 결과를 있는 그대로 보고 (실패도 숨기지 않음)
5. 사용자가 직접 클릭·설정해야 할 것을 정확히 안내 (계정, 키 등록, 결제 등)
6. 무관한 기능은 변경하지 않음. 필요해 보이면 제안만 하고 승인을 받음

**API 키/비밀번호는 채팅에 붙여넣지 마세요.** Vercel·Supabase의 환경변수 화면에 직접 입력하는 방법을 그때그때 안내하겠습니다.
